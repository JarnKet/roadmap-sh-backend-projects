<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta
            name="description"
            content="Welcome to our personal blog. Read the latest articles and insights."
        />
        <title>
            <%= locals.title || 'Latest Articles' %> - <%= siteName %>
        </title>
        <link rel="stylesheet" href="/public/css/styles.css" />
    </head>
    <body>
        <header class="header">
            <div class="container">
                <div class="header-content">
                    <a href="/home" class="logo"><%= siteName %></a>
                    <nav>
                        <ul class="nav-links">
                            <li><a href="/home">Home</a></li>
                            <% if (locals.isAdmin) { %>
                            <li><a href="/admin">Dashboard</a></li>
                            <li><a href="/articles/new">New Article</a></li>
                            <% } else { %>
                            <li><a href="/admin">Admin</a></li>
                            <% } %>
                        </ul>
                    </nav>
                </div>
            </div>
        </header>

        <main class="content">
            <div class="container">
                <div class="card">
                    <div class="card-header text-center">
                        <h1>Welcome to My Personal Blog</h1>
                        <p class="card-meta">
                            Sharing thoughts, insights, and experiences
                        </p>
                    </div>
                </div>

                <% if (articles && articles.length > 0) { %>
                <h2>Latest Articles</h2>
                <ul class="article-list">
                    <% articles.forEach(article => { %>
                    <li class="article-item">
                        <h3 class="article-title">
                            <a href="/articles/<%= article.id %>"
                                ><%= article.title %></a
                            >
                        </h3>
                        <div class="article-meta">
                            Published on <%= new
                            Date(article.publishDate).toLocaleDateString('en-US',
                            { year: 'numeric', month: 'long', day: 'numeric' })
                            %> <% if (article.updatedAt && article.updatedAt !==
                            article.createdAt) { %> • Updated <%= new
                            Date(article.updatedAt).toLocaleDateString('en-US',
                            { year: 'numeric', month: 'long', day: 'numeric' })
                            %> <% } %>
                        </div>
                        <div class="article-content">
                            <p>
                                <%= article.content.substring(0, 200) %><%=
                                article.content.length > 200 ? '...' : '' %>
                            </p>
                        </div>
                        <div class="article-actions">
                            <a
                                href="/articles/<%= article.id %>"
                                class="btn btn-primary btn-sm"
                                >Read More</a
                            >
                        </div>
                    </li>
                    <% }) %>
                </ul>
                <% } else { %>
                <div class="card text-center">
                    <div class="card-content">
                        <h3>No Articles Yet</h3>
                        <p>
                            There are no published articles at the moment. Check
                            back later for new content!
                        </p>
                        <% if (locals.isAdmin) { %>
                        <a href="/articles/new" class="btn btn-primary"
                            >Write First Article</a
                        >
                        <% } %>
                    </div>
                </div>
                <% } %>
            </div>
        </main>

        <footer class="footer">
            <div class="container">
                <p>
                    &copy; <%= currentYear %> <%= siteName %>. Built with
                    Node.js and Express.
                </p>
            </div>
        </footer>
    </body>
</html>
